# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# FastAPI Backend Configuration
# IMPORTANT: All authentication now goes through FastAPI backend
NEXT_PUBLIC_API_URL=http://localhost:8000

# Better Auth Configuration
# Better Auth is now a thin UI/session layer that delegates to FastAPI
# Secret still required for Better Auth internal operations (session signing, etc.)
# Generate a secure secret with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-super-secret-key-min-32-chars-change-this-in-production

# Environment
NEXT_PUBLIC_ENVIRONMENT=development

# Chatbot API Configuration
# The AI chatbot backend runs on a separate port
# Make sure to start the chatbot backend on this port: uvicorn app.main:app --port 8001
NEXT_PUBLIC_CHATBOT_API_URL=http://localhost:8001
