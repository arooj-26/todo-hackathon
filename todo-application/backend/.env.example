# Database Configuration
# For Neon PostgreSQL with pg8000 driver (pure Python, Windows compatible)
DATABASE_URL=postgresql+pg8000://user:password@ep-example.us-east-2.aws.neon.tech/neondb
# For local PostgreSQL
# DATABASE_URL=postgresql+pg8000://todouser:todopassword@localhost:5432/tododb

# Security - JWT Configuration
# CRITICAL: Generate a new key for each environment using one of these methods:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# openssl rand -hex 64
# python -c "import secrets; print(secrets.token_hex(64))"
# Minimum 64 bytes (128 hex characters) for HS256 algorithm
SECRET_KEY=your-secret-key-REPLACE-WITH-128-char-hex-string-use-command-above
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_HOURS=24

# CORS - Allowed Origins
ALLOWED_ORIGINS=["http://localhost:3000"]

# Application
PROJECT_NAME=Todo API
VERSION=1.0.0
API_PREFIX=/api

# Environment
ENVIRONMENT=development
