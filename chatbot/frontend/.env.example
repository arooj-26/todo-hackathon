# Backend API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8001

# OpenAI ChatKit Configuration (REQUIRED)
# This application uses OpenAI ChatKit exclusively
# Get your domain key from: https://platform.openai.com/settings/organization/security/domain-allowlist
#
# Steps to configure:
# 1. Go to https://platform.openai.com/settings/organization/security/domain-allowlist
# 2. Click "Add domain"
# 3. Add your domain (e.g., localhost:3000 for dev, your-app.vercel.app for prod)
# 4. Copy the provided domain key
# 5. Paste it below (replace "your-domain-key-here" with your actual key)
#
# IMPORTANT: The application will not work without a valid ChatKit domain key
NEXT_PUBLIC_OPENAI_DOMAIN_KEY=your-domain-key-here

# User Configuration
# Default user ID (placeholder for MVP - will be replaced with proper authentication)
NEXT_PUBLIC_DEFAULT_USER_ID=00000000-0000-0000-0000-000000000000
