/**
 * Root layout component for Next.js 14 App Router.
 * Minimal layout for landing and auth pages
 */

import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Providers } from './providers';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'TodoFlow - AI-Powered Task Management',
  description: 'Manage tasks with AI-powered intelligence. Chat naturally, set recurring tasks, and never miss a deadline.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  );
}
